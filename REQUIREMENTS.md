# Требования к сервису OpenShift Controller

## Общее описание

Сервис должен предоставлять удобный веб-интерфейс для управления сервисами и подами в OpenShift кластере с возможностью массовых операций.

## Функциональные требования

### 1. Ребут сервисов (Deployments)

#### 1.1. Выборочный ребут сервисов
- **Описание**: Пользователь может выбрать несколько сервисов (через галочки) и перезапустить их одновременно
- **Интерфейс**: 
  - Список всех Deployments в namespace
  - Чекбоксы для выбора нужных сервисов
  - Кнопка "Перезапустить выбранные"
- **Логика**:
  - При выборе сервисов и нажатии кнопки выполняется перезапуск всех выбранных Deployments
  - Перезапуск выполняется через `oc rollout restart deployment/<name>` или удаление всех подов Deployment'а
  - Показывается прогресс операции
  - Отображается результат (успех/ошибка для каждого сервиса)

#### 1.2. Ребут всего кластера
- **Описание**: Одной кнопкой перезапустить все сервисы (Deployments) в namespace
- **Интерфейс**:
  - Кнопка "Перезапустить весь кластер" / "Reboot All"
  - Подтверждение действия (модальное окно с предупреждением)
- **Логика**:
  - Находит все Deployments в namespace
  - Выполняет перезапуск каждого Deployment'а
  - Показывает прогресс и результаты

### 2. Установка количества подов (Scale)

#### 2.1. Масштабирование отдельных сервисов
- **Описание**: Пользователь может установить желаемое количество реплик для каждого сервиса
- **Интерфейс**:
  - Для каждого Deployment показывается текущее количество подов
  - Поле ввода для нового количества (0, 1, 2, ... 20, и т.д.)
  - Кнопка "Сохранить" / "Применить" для каждого сервиса
- **Логика**:
  - При изменении значения и нажатии "Сохранить" выполняется `oc scale deployment/<name> --replicas=<count>`
  - Обновляется отображение текущего количества подов
  - Показывается статус операции (успех/ошибка)

#### 2.2. Массовое масштабирование
- **Описание**: Установить одинаковое количество реплик для нескольких выбранных сервисов
- **Интерфейс**:
  - Чекбоксы для выбора сервисов
  - Общее поле ввода для количества реплик
  - Кнопка "Применить к выбранным"
- **Логика**:
  - Применяет указанное количество реплик ко всем выбранным Deployment'ам
  - Показывает прогресс и результаты

### 3. Управление изначальным состоянием

#### 3.1. Сохранение изначального состояния
- **Описание**: Система должна запоминать изначальное количество реплик для каждого Deployment'а
- **Логика**:
  - При первом запуске или по запросу сохраняется текущее состояние всех Deployment'ов
  - Сохраняется: имя Deployment'а, namespace, изначальное количество реплик
  - Данные хранятся в памяти (можно расширить до БД/файла)
  - Показывается изначальное количество рядом с текущим

#### 3.2. Установка всех в 0 (Shutdown All)
- **Описание**: Одной кнопкой установить количество реплик = 0 для всех сервисов
- **Интерфейс**:
  - Кнопка "Остановить все" / "Shutdown All" / "Установить все в 0"
  - Подтверждение действия
- **Логика**:
  - Находит все Deployments в namespace
  - Перед установкой в 0 сохраняет текущее состояние (если еще не сохранено)
  - Устанавливает `replicas=0` для всех Deployment'ов
  - Показывает прогресс операции

#### 3.3. Восстановление изначального состояния
- **Описание**: Восстановить все сервисы в изначальное состояние (количество реплик)
- **Интерфейс**:
  - Кнопка "Восстановить все" / "Restore All" / "Вернуть изначальное состояние"
  - Показывается список того, что будет восстановлено
- **Логика**:
  - Берет сохраненное изначальное состояние
  - Восстанавливает количество реплик для каждого Deployment'а
  - Если для какого-то Deployment'а нет сохраненного состояния, пропускает его или использует значение по умолчанию
  - Показывает прогресс и результаты

## Технические требования

### API Endpoints

#### Deployment Management
- `GET /api/deployments/{namespace}` - получить список всех Deployments
- `GET /api/deployments/{namespace}/{name}` - получить информацию о Deployment
- `POST /api/deployments/{namespace}/{name}/restart` - перезапустить Deployment
- `POST /api/deployments/{namespace}/restart-all` - перезапустить все Deployments
- `POST /api/deployments/{namespace}/restart-selected` - перезапустить выбранные Deployments
- `PUT /api/deployments/{namespace}/{name}/scale` - масштабировать Deployment
- `POST /api/deployments/{namespace}/scale-all` - масштабировать все Deployment'ы
- `POST /api/deployments/{namespace}/shutdown-all` - установить все в 0
- `POST /api/deployments/{namespace}/restore` - восстановить изначальное состояние
- `POST /api/deployments/{namespace}/save-state` - сохранить текущее состояние

### Модели данных

#### DeploymentInfo
```java
{
  name: String,
  namespace: String,
  currentReplicas: Integer,
  desiredReplicas: Integer,
  availableReplicas: Integer,
  readyReplicas: Integer,
  originalReplicas: Integer,  // сохраненное изначальное значение
  labels: Map<String, String>,
  creationTimestamp: Instant,
  status: String
}
```

#### StateSnapshot
```java
{
  namespace: String,
  timestamp: Instant,
  deployments: Map<String, Integer>  // deployment name -> replica count
}
```

### Хранение состояния

**Вариант 1: В памяти (простой)**
- Использовать `ConcurrentHashMap` для хранения изначальных состояний
- Ключ: `namespace/deployment-name`
- Значение: изначальное количество реплик
- Плюсы: простота, быстрота
- Минусы: теряется при перезапуске сервиса

**Вариант 2: В файле (рекомендуется)**
- Сохранять состояние в JSON файл `state-snapshots.json`
- При старте загружать сохраненные состояния
- Плюсы: сохраняется между перезапусками
- Минусы: нужно управлять файлом

**Вариант 3: В БД (для production)**
- Использовать H2/PostgreSQL для хранения состояний
- Плюсы: надежность, история изменений
- Минусы: сложнее в настройке

## План реализации

### Этап 1: Базовая работа с Deployments
- [ ] Создать `DeploymentService` для работы с Deployments API
- [ ] Реализовать получение списка Deployments
- [ ] Реализовать получение информации о Deployment
- [ ] Реализовать масштабирование Deployment (scale)
- [ ] Реализовать перезапуск Deployment (restart)

### Этап 2: Массовые операции
- [ ] Реализовать перезапуск всех Deployments
- [ ] Реализовать перезапуск выбранных Deployments
- [ ] Реализовать масштабирование всех Deployments
- [ ] Реализовать установку всех в 0

### Этап 3: Управление состоянием
- [ ] Создать `StateService` для сохранения/восстановления состояний
- [ ] Реализовать сохранение изначального состояния
- [ ] Реализовать восстановление изначального состояния
- [ ] Добавить отображение изначального количества реплик

### Этап 4: Веб-интерфейс
- [ ] Создать страницу управления Deployments
- [ ] Добавить чекбоксы для выбора сервисов
- [ ] Добавить поля ввода для масштабирования
- [ ] Добавить кнопки массовых операций
- [ ] Добавить отображение изначального/текущего состояния
- [ ] Добавить модальные окна подтверждения
- [ ] Добавить отображение прогресса операций

### Этап 5: Улучшения
- [ ] Добавить фильтрацию и поиск Deployments
- [ ] Добавить группировку по labels
- [ ] Добавить историю операций
- [ ] Добавить уведомления об успехе/ошибках
- [ ] Оптимизировать производительность массовых операций

## Интерфейс пользователя

### Стартовая страница - Список проектов (Namespaces)

```
┌─────────────────────────────────────────────────────────┐
│  🚀 OpenShift Controller                               │
│  Выберите проект (namespace)                            │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  📁 diavolos88-dev                                      │
│  📁 production                                          │
│  📁 staging                                             │
│  📁 development                                         │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

### Страница управления Deployments

```
┌─────────────────────────────────────────────────────────┐
│  🚀 OpenShift Controller - diavolos88-dev             │
│  [← Назад к проектам]                                  │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  [🛑 Потушить все]  [🔄 Рестартнуть все]  [↩️ Восстановить] │
│                                                          │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────────────────────────────────────────────┐   │
│  │ Service-1                                        │   │
│  │ Текущие: 3  |  Стартовое: 3  |  [0] [Сохранить]│   │
│  └──────────────────────────────────────────────────┘   │
│                                                          │
│  ┌──────────────────────────────────────────────────┐   │
│  │ Service-2                                        │   │
│  │ Текущие: 5  |  Стартовое: 5  |  [0] [Сохранить] │   │
│  └──────────────────────────────────────────────────┘   │
│                                                          │
│  ┌──────────────────────────────────────────────────┐   │
│  │ Service-3                                        │   │
│  │ Текущие: 0  |  Стартовое: 2  |  [2] [Сохранить] │   │
│  └──────────────────────────────────────────────────┘   │
│                                                          │
│  ┌──────────────────────────────────────────────────┐   │
│  │ Ingress-Controller                               │   │
│  │ Текущие: 1  |  Стартовое: 1  |  [1] [Сохранить] │   │
│  └──────────────────────────────────────────────────┘   │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

**Описание элементов:**
- **Название Deployment** - отображается крупным шрифтом
- **Текущие** - текущее количество реплик (желаемое количество)
- **Стартовое** - изначальное количество реплик (сохраненное при первом открытии)
- **Поле ввода** - числовое поле для ввода нового количества (по умолчанию текущее значение)
- **Кнопка "Сохранить"** - применяет новое количество реплик к этому Deployment
- **Кнопки сверху**:
  - **Потушить все** - устанавливает все Deployment'ы в 0 реплик
  - **Рестартнуть все** - перезапускает все Deployment'ы
  - **Восстановить** - восстанавливает стартовое значение для всех Deployment'ов

## Логика работы

### Сохранение состояния

1. Пользователь открывает страницу управления
2. Система проверяет, есть ли сохраненное состояние для namespace
3. Если нет - автоматически сохраняет текущее состояние всех Deployments
4. Отображает изначальное количество рядом с текущим

### Установка всех в 0

1. Пользователь нажимает "Остановить все"
2. Показывается подтверждение с предупреждением
3. Если подтверждено:
   - Сохраняется текущее состояние (если еще не сохранено)
   - Для каждого Deployment устанавливается `replicas=0`
   - Показывается прогресс
   - Отображается результат

### Восстановление

1. Пользователь нажимает "Восстановить все"
2. Система берет сохраненное изначальное состояние
3. Для каждого Deployment восстанавливает количество реплик
4. Показывается прогресс и результаты

### Перезапуск

1. Для Deployment: удаляются все поды, ReplicaSet автоматически создает новые
2. Для нескольких: выполняется параллельно для всех выбранных
3. Для всех: выполняется для всех Deployments в namespace

## Безопасность

- Все операции требуют подтверждения
- Опасные операции (остановить все, перезапустить все) требуют двойного подтверждения
- Логирование всех операций
- Проверка прав доступа к namespace

## Производительность

- Массовые операции выполняются параллельно (используя CompletableFuture)
- Ограничение на количество одновременных операций (например, не более 10)
- Кэширование списка Deployments (обновление каждые 30 секунд)
- Асинхронное выполнение длительных операций с отображением прогресса

