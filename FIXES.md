# История исправлений

## 2025-11-19 - Исправление ошибки 403 Forbidden при получении списка Namespaces

### Проблема
При запуске приложения возникала ошибка 403 Forbidden при попытке получить список всех namespace на уровне кластера:

```
namespaces is forbidden: User "diavolos88" cannot list resource "namespaces" in API group "" at the cluster scope
```

Ошибка возникала в методе `NamespaceService.getAllNamespaces()` при попытке получить список всех namespace через `openShiftClient.namespaces().list()`.

### Причина
У пользователя нет прав (RBAC) на получение списка всех namespace на уровне кластера. У пользователя есть права только на конкретные namespace (в данном случае `diavolos88-dev`).

### Решение
Реализована обработка ошибки 403 Forbidden в методе `NamespaceService.getAllNamespaces()`:

1. **Обработка исключения KubernetesClientException**: При получении 403 ошибки метод теперь не падает, а обрабатывает её корректно.

2. **Fallback на namespace из конфигурации**: Если возникает ошибка 403, метод возвращает список с одним namespace из конфигурации (`openshift.namespace` в `application.yml`).

3. **Проверка доступности namespace**: Перед возвратом namespace из конфигурации выполняется проверка его доступности через `openShiftClient.namespaces().withName(defaultNamespace).get()`.

4. **Улучшенное логирование**: Добавлены информативные сообщения в лог для отслеживания того, что происходит:
   - Предупреждение при 403 ошибке
   - Информация о использовании namespace из конфигурации
   - Проверка доступности namespace

### Изменения в коде

**Файл**: `src/main/java/com/openshift/controller/service/NamespaceService.java`

- Добавлено поле `defaultNamespace` с аннотацией `@Value("${openshift.namespace:default}")` для получения namespace из конфигурации
- Добавлена обработка `KubernetesClientException` в методе `getAllNamespaces()`
- При 403 ошибке возвращается список с одним доступным namespace из конфигурации
- Добавлена проверка доступности namespace перед возвратом

### Статус
✅ **Исправлено** - Теперь приложение корректно работает с ограниченными правами пользователя и показывает только доступный namespace.

### Дополнительная информация
Это исправление позволяет приложению работать в окружениях с ограниченными правами, где у пользователя нет доступа на уровне кластера, но есть права на конкретные namespace.

---

## 2025-11-19 - Минималистичный формат отображения подов (одна строка на под)

### Проблема
При большом количестве подов (более 20) существующий формат отображения с карточками занимал много места на экране и было сложно быстро просматривать информацию о всех подах.

### Решение
Переработан формат отображения подов на более компактный и минималистичный:

1. **Однострочный формат**: Все данные о каждом поде теперь отображаются в одной строке
2. **Заголовки колонок**: Добавлена строка заголовков для лучшей читаемости (Имя, Статус, Namespace, Node, Создан, Действия)
3. **Компактные кнопки**: Кнопки действий заменены на иконки (📄 для деталей, 🔄 для перезапуска, 🗑️ для удаления) с подсказками при наведении
4. **Оптимизированные отступы**: Уменьшены отступы и размеры элементов для более компактного отображения
5. **Гибкая сетка**: Использован flexbox для равномерного распределения колонок

### Изменения в коде

**Файл**: `src/main/resources/templates/index.html`

- Заменены карточки (`.pod-card`) на однострочные элементы (`.pod-row`)
- Добавлена строка заголовков (`.pod-header-row`)
- Все данные (имя, статус, namespace, node, дата) расположены горизонтально в одной строке
- Кнопки действий заменены на компактные иконки
- Уменьшены размеры шрифтов и отступов для более плотного размещения

### Статус
✅ **Реализовано** - Теперь список подов отображается в компактном однострочном формате, что позволяет легко просматривать большое количество подов.

### Результат
- Все данные о поде умещаются в одну строку
- Удобный просмотр большого количества подов (20+)
- Понятная структура с заголовками колонок
- Компактные кнопки действий с иконками

---

## 2025-11-19 - Минималистичный формат отображения деплойментов (одна строка на деплоймент)

### Проблема
Страница деплойментов (deployments) отображала каждый деплоймент в виде карточки с несколькими строками информации. При большом количестве деплойментов (20+) такой формат занимал много места и было сложно быстро просматривать информацию.

### Решение
Применён тот же минималистичный подход к странице деплойментов:

1. **Однострочный формат**: Все данные о каждом деплойменте теперь отображаются в одной строке
2. **Заголовки колонок**: Добавлена строка заголовков (Имя | Текущие | Стартовое | Готовые | Масштаб)
3. **Компактное поле ввода**: Поле ввода количества реплик уменьшено до 60px
4. **Иконка сохранения**: Кнопка "Сохранить" заменена на иконку 💾
5. **Гибкая сетка**: Использован flexbox для равномерного распределения колонок

### Изменения в коде

**Файл**: `src/main/resources/templates/deployments.html`

- Заменены карточки (`.deployment-card`) на однострочные элементы (`.deployment-row`)
- Добавлена строка заголовков (`.deployment-header-row`)
- Все данные (имя, текущие, стартовое, готовые реплики, масштаб) расположены горизонтально в одной строке
- Кнопка "Сохранить" заменена на компактную иконку 💾

### Статус
✅ **Реализовано** - Теперь список деплойментов отображается в компактном однострочном формате, что позволяет легко просматривать большое количество деплойментов.

### Результат
- Все данные о деплойменте умещаются в одну строку
- Удобный просмотр большого количества деплойментов (20+)
- Понятная структура с заголовками колонок
- Компактное поле ввода и кнопка сохранения

