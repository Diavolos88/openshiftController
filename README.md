# OpenShift Controller Service

Сервис на Java Spring Boot для управления подами и сервисами (Deployments) в OpenShift через Kubernetes API.

> 📋 **Требования и план реализации**: См. [REQUIREMENTS.md](REQUIREMENTS.md) и [IMPLEMENTATION_PLAN.md](IMPLEMENTATION_PLAN.md)

> ⚠️ **Правила разработки**: См. [PROJECT_RULES.md](PROJECT_RULES.md) - **обязательно перезапускайте проект после изменений!**

## Возможности

### Управление подами
- ✅ Получение списка всех подов в namespace
- ✅ Получение детальной информации о конкретном поде
- ✅ Перезапуск подов (удаление с автоматическим пересозданием через Deployment)
- ✅ Полное удаление подов
- ✅ Поиск подов по label selector
- ✅ **Массовые операции** - перезапуск и удаление выбранных подов

### Управление Deployments
- ✅ Просмотр списка всех проектов (namespaces)
- ✅ Просмотр всех Deployments в проекте
- ✅ Масштабирование отдельных Deployments
- ✅ **Массовое масштабирование** - изменение количества реплик для нескольких Deployments одновременно
- ✅ **Кнопка "Сохранить все"** - сохранение всех измененных значений реплик одной кнопкой
- ✅ Перезапуск всех Deployments
- ✅ Перезапуск выбранных Deployments
- ✅ Остановка всех Deployments (установка в 0)
- ✅ Остановка выбранных Deployments (установка в 0)
- ✅ Восстановление изначального состояния
- ✅ Восстановление выбранных Deployments
- ✅ Автоматическое сохранение изначального состояния
- ✅ Обновление стартовых значений

### Управление подключениями и группами
- ✅ Добавление нескольких подключений к разным OpenShift кластерам
- ✅ Переключение между подключениями
- ✅ Редактирование и удаление подключений
- ✅ Хранение конфигураций в базе данных PostgreSQL
- ✅ **Mock-подключения для тестирования** - создание тестовых подключений с заглушками данных
- ✅ **Группы подключений** - организация подключений в группы для удобного управления
- ✅ **Массовые операции в группах** - выполнение операций над выбранными Deployments из разных подключений в группе одной кнопкой
- ✅ **Общая кнопка "Сохранить все" в группах** - сохранение изменений реплик для всех измененных Deployments из всех подключений в группе

## Технологии

- **Java 17**
- **Spring Boot 3.2.0**
- **Fabric8 Kubernetes Client 6.9.2** - библиотека для работы с Kubernetes/OpenShift API
- **PostgreSQL** - база данных для хранения конфигураций подключений
- **Spring Data JPA** - для работы с базой данных
- **Thymeleaf** - шаблонизатор для веб-интерфейса
- **Maven** - система сборки

## Требования

1. Java 17 или выше
2. Maven 3.6+
3. PostgreSQL 12+ (для хранения конфигураций подключений)
4. Доступ к OpenShift/Kubernetes кластеру
5. Токен доступа к OpenShift кластеру

> **💡 Для локального тестирования:** См. [LOCAL_SETUP.md](LOCAL_SETUP.md) - инструкции по развертыванию локального OpenShift/Kubernetes кластера (Minikube, Kind, OpenShift Local и др.)

> **💡 Для Red Hat Sandbox:** См. [SANDBOX_SETUP.md](SANDBOX_SETUP.md) - инструкции по подключению к Red Hat Sandbox (облачный OpenShift кластер)

## Настройка

### 1. Настройка базы данных PostgreSQL

Создайте базу данных:

```sql
CREATE DATABASE openshift_controller;
```

Настройте подключение в `src/main/resources/application.yml`:

```yaml
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/openshift_controller
    username: postgres
    password: your-password
```

### 2. Настройка подключения к OpenShift

**Важно:** Конфигурация подключения к OpenShift теперь настраивается через веб-интерфейс, а не через файлы конфигурации.

1. Запустите приложение
2. Откройте `http://localhost:8080`
3. Вы будете перенаправлены на страницу настройки подключения
4. Заполните форму:
   - URL кластера OpenShift
   - Токен аутентификации
   - Namespace по умолчанию (необязательно)
   - Название подключения (необязательно)

Подробнее см. [DATABASE_MIGRATION.md](DATABASE_MIGRATION.md)

### 3. Тестовые mock-подключения

Для визуального тестирования интерфейса без реального OpenShift кластера можно создать mock-подключение:

1. На странице управления подключениями (`/connection/setup`)
2. Нажмите кнопку **"Создать тестовое подключение"**
3. Mock-подключение будет создано и активировано автоматически

Mock-подключение предоставляет:
- Тестовые namespaces: `test-project`, `staging`, `production`, `development`
- Тестовые поды в каждом namespace с различными статусами
- Тестовые deployments с реалистичными данными
- Все операции (перезапуск, масштабирование и т.д.) работают в режиме заглушки

**Важно:** Mock-подключения используются только для визуального тестирования интерфейса и не требуют реального OpenShift кластера.

## Сборка и запуск

### Сборка проекта

```bash
mvn clean package
```

### Запуск приложения

```bash
mvn spring-boot:run
```

Или запуск собранного JAR:

```bash
java -jar target/openshift-controller-1.0.0.jar
```

Приложение запустится на порту 8080.

## API Endpoints

### 1. Получить список всех подов

```http
GET /api/pods/{namespace}
```

**Пример:**
```bash
curl http://localhost:8080/api/pods/my-namespace
```

**Ответ:**
```json
{
  "success": true,
  "message": "Найдено 3 подов в namespace my-namespace",
  "data": [
    {
      "name": "my-app-12345-abc",
      "namespace": "my-namespace",
      "status": "Running",
      "nodeName": "node1",
      "creationTimestamp": "2024-01-15T10:30:00Z",
      "labels": {
        "app": "my-app",
        "version": "1.0"
      }
    }
  ]
}
```

### 2. Получить информацию о конкретном поде

```http
GET /api/pods/{namespace}/{podName}
```

**Пример:**
```bash
curl http://localhost:8080/api/pods/my-namespace/my-app-12345-abc
```

### 3. Перезапустить под

```http
POST /api/pods/{namespace}/{podName}/restart
```

**Пример:**
```bash
curl -X POST http://localhost:8080/api/pods/my-namespace/my-app-12345-abc/restart
```

**Важно:** Перезапуск работает через удаление пода. Если под управляется через Deployment, он будет автоматически пересоздан.

### 4. Удалить под

```http
DELETE /api/pods/{namespace}/{podName}
```

**Пример:**
```bash
curl -X DELETE http://localhost:8080/api/pods/my-namespace/my-app-12345-abc
```

### 5. Найти поды по label selector

```http
GET /api/pods/{namespace}/search?labelSelector={selector}
```

**Пример:**
```bash
curl "http://localhost:8080/api/pods/my-namespace/search?labelSelector=app=myapp"
```

## Структура проекта

```
openshiftController/
├── src/
│   ├── main/
│   │   ├── java/com/openshift/controller/
│   │   │   ├── OpenShiftControllerApplication.java  # Главный класс приложения
│   │   │   ├── config/
│   │   │   │   └── OpenShiftConfig.java            # Конфигурация подключения к OpenShift
│   │   │   ├── controller/
│   │   │   │   └── PodController.java              # REST контроллер
│   │   │   ├── service/
│   │   │   │   └── PodService.java                 # Бизнес-логика работы с подами
│   │   │   └── dto/
│   │   │       ├── PodInfo.java                    # DTO для информации о поде
│   │   │       └── ApiResponse.java                # Стандартный ответ API
│   │   └── resources/
│   │       └── application.yml                     # Конфигурация приложения
│   └── test/                                       # Тесты (будут добавлены)
├── pom.xml                                         # Maven конфигурация
└── README.md                                       # Документация
```

## Как это работает

1. **Подключение к кластеру**: Приложение использует Fabric8 Kubernetes Client для подключения к OpenShift/Kubernetes API. Поддерживается несколько способов аутентификации (kubeconfig, токен, переменные окружения).

2. **REST API**: Spring Boot предоставляет REST endpoints для всех операций с подами.

3. **Перезапуск подов**: В Kubernetes/OpenShift нет прямого API для перезапуска пода. Перезапуск реализован через удаление пода. Если под управляется через Deployment или ReplicaSet, контроллер автоматически создаст новый под.

4. **Безопасность**: Убедитесь, что у сервиса есть необходимые права (RBAC) в кластере для выполнения операций с подами.

## RBAC настройки (для OpenShift/Kubernetes)

Для работы сервиса необходимы следующие права:

```yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: pod-manager
rules:
- apiGroups: [""]
  resources: ["pods"]
  verbs: ["get", "list", "delete", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: pod-manager-binding
subjects:
- kind: ServiceAccount
  name: openshift-controller
  namespace: default
roleRef:
  kind: ClusterRole
  name: pod-manager
  apiGroup: rbac.authorization.k8s.io
```

## Логирование

Логирование настроено в `application.yml`. Уровни логирования:
- `com.openshift` - DEBUG (детальные логи работы сервиса)
- `io.fabric8` - INFO (логи Kubernetes клиента)
- `org.springframework` - INFO (логи Spring)

## Мониторинг

Приложение включает Spring Boot Actuator для мониторинга:

- Health check: `http://localhost:8080/actuator/health`
- Metrics: `http://localhost:8080/actuator/metrics`
- Info: `http://localhost:8080/actuator/info`

## Дальнейшее развитие

Возможные улучшения:
- [ ] Управление ConfigMap и Secrets
- [ ] Мониторинг состояния подов в реальном времени
- [ ] WebSocket для стриминга логов подов
- [ ] Аутентификация и авторизация в REST API
- [ ] Интеграция с OpenShift OAuth
- [ ] История операций
- [ ] Экспорт/импорт конфигураций подключений

## Лицензия

MIT

