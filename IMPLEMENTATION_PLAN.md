# План реализации расширенного функционала

## Текущий статус

✅ Реализовано:
- Базовое управление подами (получение, перезапуск, удаление)
- Веб-интерфейс для просмотра подов
- Подключение к OpenShift через токен

## Этап 1: Работа с Deployments (Приоритет: ВЫСОКИЙ)

### 1.1. Создать DTO для Deployment
**Файл**: `src/main/java/com/openshift/controller/dto/DeploymentInfo.java`

```java
@Data
@Builder
public class DeploymentInfo {
    private String name;
    private String namespace;
    private Integer currentReplicas;      // Текущее желаемое количество
    private Integer availableReplicas;     // Доступные поды
    private Integer readyReplicas;         // Готовые поды
    private Integer originalReplicas;      // Изначальное количество (сохраненное)
    private Map<String, String> labels;
    private Instant creationTimestamp;
    private String status;
}
```

### 1.2. Создать DeploymentService
**Файл**: `src/main/java/com/openshift/controller/service/DeploymentService.java`

**Методы**:
- `List<DeploymentInfo> getAllDeployments(String namespace)` - получить все Deployments
- `DeploymentInfo getDeployment(String namespace, String name)` - получить Deployment
- `boolean scaleDeployment(String namespace, String name, int replicas)` - масштабировать
- `boolean restartDeployment(String namespace, String name)` - перезапустить
- `Map<String, Boolean> restartDeployments(String namespace, List<String> names)` - перезапустить несколько
- `Map<String, Boolean> restartAllDeployments(String namespace)` - перезапустить все
- `Map<String, Boolean> scaleAllDeployments(String namespace, int replicas)` - масштабировать все

### 1.3. Создать REST API для Deployments
**Файл**: `src/main/java/com/openshift/controller/controller/DeploymentController.java`

**Endpoints**:
- `GET /api/deployments/{namespace}` - список
- `GET /api/deployments/{namespace}/{name}` - детали
- `POST /api/deployments/{namespace}/{name}/restart` - перезапуск
- `POST /api/deployments/{namespace}/restart-all` - перезапуск всех
- `POST /api/deployments/{namespace}/restart-selected` - перезапуск выбранных
- `PUT /api/deployments/{namespace}/{name}/scale?replicas={count}` - масштабирование
- `POST /api/deployments/{namespace}/scale-all?replicas={count}` - масштабировать все

**Время**: ~2-3 часа

---

## Этап 2: Управление состоянием (Приоритет: ВЫСОКИЙ)

### 2.1. Создать StateService
**Файл**: `src/main/java/com/openshift/controller/service/StateService.java`

**Функционал**:
- Сохранение изначального состояния всех Deployments в namespace
- Восстановление изначального состояния
- Хранение в памяти (ConcurrentHashMap) + опционально в файле

**Методы**:
- `void saveState(String namespace)` - сохранить текущее состояние
- `Map<String, Integer> getOriginalState(String namespace)` - получить сохраненное состояние
- `boolean restoreState(String namespace)` - восстановить состояние
- `Integer getOriginalReplicas(String namespace, String deploymentName)` - получить изначальное количество для конкретного Deployment

**Хранение**:
```java
// В памяти
private final ConcurrentHashMap<String, Map<String, Integer>> stateStorage = new ConcurrentHashMap<>();
// Ключ: namespace, Значение: Map<deploymentName, originalReplicas>
```

**Опционально - сохранение в файл**:
- Файл: `state-snapshots.json`
- Формат: JSON с timestamp и состояниями по namespace
- Загрузка при старте приложения

**Время**: ~2 часа

### 2.2. Интеграция с DeploymentService
- При получении списка Deployments добавлять `originalReplicas` из StateService
- При сохранении состояния вызывать StateService

**Время**: ~30 минут

---

## Этап 3: Веб-интерфейс управления (Приоритет: ВЫСОКИЙ)

### 3.1. Создать Web контроллер для Deployments
**Файл**: `src/main/java/com/openshift/controller/controller/DeploymentWebController.java`

**Методы**:
- `GET /deployments` - главная страница управления
- `POST /deployments/{namespace}/restart-all` - перезапустить все
- `POST /deployments/{namespace}/shutdown-all` - установить все в 0
- `POST /deployments/{namespace}/restore` - восстановить
- `POST /deployments/{namespace}/save-state` - сохранить состояние

**Время**: ~1 час

### 3.2. Создать HTML шаблон
**Файл**: `src/main/resources/templates/deployments.html`

**Элементы**:
- Список всех Deployments с чекбоксами
- Отображение текущего и изначального количества реплик
- Поля ввода для масштабирования каждого сервиса
- Кнопки массовых операций
- Модальные окна для подтверждения

**Время**: ~3-4 часа

### 3.3. JavaScript для интерактивности
**Файл**: `src/main/resources/static/js/deployments.js` (опционально, можно встроить в HTML)

**Функционал**:
- Обработка чекбоксов
- AJAX запросы для операций
- Отображение прогресса
- Обновление данных без перезагрузки страницы

**Время**: ~2 часа

---

## Этап 4: Массовые операции (Приоритет: СРЕДНИЙ)

### 4.1. Параллельное выполнение операций
**В DeploymentService**:
- Использовать `CompletableFuture` для параллельного выполнения
- Ограничение на количество одновременных операций (ThreadPoolExecutor)
- Обработка ошибок для каждой операции отдельно

**Пример**:
```java
public Map<String, Boolean> restartAllDeployments(String namespace) {
    List<DeploymentInfo> deployments = getAllDeployments(namespace);
    ExecutorService executor = Executors.newFixedThreadPool(10);
    
    List<CompletableFuture<Map.Entry<String, Boolean>>> futures = 
        deployments.stream()
            .map(d -> CompletableFuture.supplyAsync(() -> {
                boolean success = restartDeployment(namespace, d.getName());
                return Map.entry(d.getName(), success);
            }, executor))
            .toList();
    
    return CompletableFuture.allOf(futures.toArray(new CompletableFuture[0]))
        .thenApply(v -> futures.stream()
            .map(CompletableFuture::join)
            .collect(Collectors.toMap(Map.Entry::getKey, Map.Entry::getValue)))
        .join();
}
```

**Время**: ~2 часа

### 4.2. Отображение прогресса
- WebSocket или Server-Sent Events для обновления прогресса в реальном времени
- Или простое обновление через AJAX каждые 2 секунды

**Время**: ~2-3 часа (опционально)

---

## Этап 5: Улучшения и оптимизация (Приоритет: НИЗКИЙ)

### 5.1. Кэширование
- Кэшировать список Deployments (TTL 30 секунд)
- Использовать Spring Cache

**Время**: ~1 час

### 5.2. Фильтрация и поиск
- Поиск по имени Deployment
- Фильтрация по labels
- Сортировка

**Время**: ~1-2 часа

### 5.3. История операций
- Логирование всех операций
- Отображение последних операций в UI

**Время**: ~2 часа

---

## Порядок реализации

### Спринт 1 (День 1-2) ✅ ВЫПОЛНЕНО
1. ✅ Создать DeploymentInfo DTO
2. ✅ Создать DeploymentService с базовыми методами
3. ✅ Создать StateService
4. ✅ Создать NamespaceService
5. ✅ Интегрировать сохранение/восстановление состояния
6. ✅ Добавить originalReplicas в DeploymentInfo

### Спринт 2 (День 3) ✅ ВЫПОЛНЕНО
7. ✅ Создать DeploymentWebController
8. ✅ Создать веб-интерфейс (HTML шаблоны)
   - ✅ namespaces.html - список проектов
   - ✅ deployments.html - управление Deployments
9. ✅ Реализовать массовые операции (restart-all, shutdown-all, restore)

### Спринт 3 (День 4) 🔄 В ПРОЦЕССЕ
10. ⏳ Протестировать все операции через UI
11. ⏳ Оптимизировать массовые операции (параллелизм)
12. ⏳ Добавить обработку ошибок
13. ⏳ Улучшить UI (автообновление, прогресс операций)

### Спринт 4 (День 5-6)
14. ⏳ Финальное тестирование
15. ⏳ Документация
16. ⏳ Оптимизация производительности

---

## Оценка времени

- **Минимальная версия (MVP)**: 2-3 дня
- **Полная версия с улучшениями**: 5-6 дней

## Зависимости

Все необходимые зависимости уже есть в проекте:
- ✅ Spring Boot Web
- ✅ Fabric8 Kubernetes Client (поддерживает Deployments)
- ✅ Thymeleaf
- ✅ Lombok

## Риски

1. **Производительность массовых операций**
   - Решение: ограничение параллелизма, таймауты

2. **Потеря состояния при перезапуске сервиса**
   - Решение: сохранение в файл или БД

3. **Права доступа в OpenShift**
   - Решение: проверка прав перед операциями, понятные сообщения об ошибках

## Тестирование

### Unit тесты
- DeploymentService методы
- StateService методы

### Integration тесты
- REST API endpoints
- Веб-контроллеры

### Ручное тестирование
- Все операции через веб-интерфейс
- Массовые операции с большим количеством Deployments
- Восстановление состояния после остановки всех

