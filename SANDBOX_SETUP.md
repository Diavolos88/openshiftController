# Подключение к Red Hat Sandbox

Этот документ описывает, как подключить наш сервис к Red Hat Sandbox (бесплатный облачный OpenShift кластер).

## Шаг 1: Получение доступа к кластеру

### Вариант A: Использовать токен и URL (проще, без kubeconfig) ⭐ РЕКОМЕНДУЕТСЯ

**Kubeconfig не обязателен!** Можно использовать только токен и URL кластера.

1. **Войдите в Red Hat Sandbox:**
   - Откройте https://developers.redhat.com/developer-sandbox
   - Войдите в свою учетную запись

2. **Откройте веб-консоль OpenShift:**
   - Нажмите "Launch" или перейдите в консоль кластера

3. **Получите токен:**
   - В правом верхнем углу нажмите на ваш логин
   - Выберите "Copy login command"
   - В открывшемся окне будет команда вида:
     ```
     oc login --token=sha256~xxxxxxxxxxxxx --server=https://api.sandbox.xxxxx.openshiftapps.com:6443
     ```
   - Скопируйте токен (часть после `--token=`) и URL сервера (часть после `--server=`)

4. **Используйте токен и URL напрямую:**
   ```powershell
   # Установите переменные окружения
   $env:OPENSHIFT_MASTER_URL = "https://api.sandbox.xxxxx.openshiftapps.com:6443"
   $env:OPENSHIFT_TOKEN = "sha256~xxxxxxxxxxxxx"
   $env:OPENSHIFT_NAMESPACE = "ваш-namespace"  # Например, ваш-username-dev
   
   # Запустите сервис - kubeconfig НЕ нужен!
   mvn spring-boot:run
   ```

### Вариант B: Скачать kubeconfig файл (альтернатива)

Если хотите использовать kubeconfig (не обязательно):

1. **Скачайте kubeconfig:**
   - В веб-консоли нажмите на ваш логин (правый верхний угол)
   - Выберите "Download kubeconfig"
   - Сохраните файл, например, в `C:\Users\salof\.kube\sandbox-config`

2. **Используйте kubeconfig:**
   ```powershell
   $env:KUBECONFIG = "C:\Users\salof\.kube\sandbox-config"
   mvn spring-boot:run
   ```

## Шаг 2: Настройка нашего сервиса

### Способ 1: Через токен и URL (БЕЗ kubeconfig) ⭐ САМЫЙ ПРОСТОЙ

```powershell
# Установите переменные окружения
$env:OPENSHIFT_MASTER_URL = "https://api.sandbox.xxxxx.openshiftapps.com:6443"
$env:OPENSHIFT_TOKEN = "sha256~ваш-токен-здесь"
$env:OPENSHIFT_NAMESPACE = "ваш-namespace"  # Например, "salof-dev"

# Запустите сервис - kubeconfig НЕ нужен!
mvn spring-boot:run
```

**Преимущества:**
- ✅ Не нужно скачивать файлы
- ✅ Не нужно настраивать kubeconfig
- ✅ Просто скопировал токен и URL - готово

### Способ 2: Через application.yml (БЕЗ kubeconfig)

Отредактируйте `src/main/resources/application.yml`:

```yaml
openshift:
  # Используйте токен и URL напрямую
  master-url: https://api.sandbox.xxxxx.openshiftapps.com:6443
  token: sha256~ваш-токен-здесь
  namespace: ваш-namespace
```

### Способ 3: Через kubeconfig (если хотите)

```powershell
# Установите путь к kubeconfig
$env:KUBECONFIG = "C:\Users\salof\.kube\sandbox-config"
mvn spring-boot:run
```

Или в `application.yml`:
```yaml
openshift:
  kubeconfig: C:\Users\salof\.kube\sandbox-config
  namespace: ваш-namespace
```

## Шаг 3: Определите namespace с вашими подами

```powershell
# Список всех namespace
kubectl get namespaces

# Список подов во всех namespace
kubectl get pods --all-namespaces

# Или в конкретном namespace
kubectl get pods -n ваш-namespace
```

**Важно:** В Red Hat Sandbox обычно namespace имеет формат `ваш-username-dev` или просто ваш username.

## Шаг 4: Запустите сервис

```powershell
# Убедитесь, что kubeconfig настроен
$env:KUBECONFIG = "C:\Users\salof\.kube\sandbox-config"

# Запустите сервис
mvn spring-boot:run
```

Если все настроено правильно, вы увидите в логах:
```
Используется стандартный kubeconfig
Успешно подключено к OpenShift кластеру. Namespace по умолчанию: ваш-namespace
```

## Шаг 5: Протестируйте API с вашими подами

### 1. Получите список ваших подов

```powershell
# Замените 'ваш-namespace' на реальный namespace
curl http://localhost:8080/api/pods/ваш-namespace
```

Или через браузер:
```
http://localhost:8080/api/pods/ваш-namespace
```

**Пример ответа:**
```json
{
  "success": true,
  "message": "Найдено 3 подов в namespace ваш-namespace",
  "data": [
    {
      "name": "pod-1-xxxxx",
      "namespace": "ваш-namespace",
      "status": "Running",
      ...
    },
    {
      "name": "pod-2-xxxxx",
      ...
    },
    {
      "name": "pod-3-xxxxx",
      ...
    }
  ]
}
```

### 2. Получите информацию о конкретном поде

```powershell
# Замените на реальные имена
curl http://localhost:8080/api/pods/ваш-namespace/pod-1-xxxxx
```

### 3. Перезапустите под

```powershell
curl -X POST http://localhost:8080/api/pods/ваш-namespace/pod-1-xxxxx/restart
```

**Важно:** Если под управляется через Deployment, он автоматически пересоздастся после удаления.

### 4. Удалите под (полное отключение)

```powershell
curl -X DELETE http://localhost:8080/api/pods/ваш-namespace/pod-1-xxxxx
```

### 5. Поиск подов по labels

```powershell
# Если у ваших подов есть labels, например app=myapp
curl "http://localhost:8080/api/pods/ваш-namespace/search?labelSelector=app=myapp"
```

## Проверка подключения перед запуском

Перед запуском сервиса убедитесь, что kubectl работает:

```powershell
# Проверьте текущий контекст
kubectl config current-context

# Должно показать что-то вроде: api-sandbox-x8i5-p1-openshiftapps-com:6443/ваш-username

# Проверьте доступ к кластеру
kubectl cluster-info

# Проверьте ваши поды
kubectl get pods -n ваш-namespace

# Должны увидеть ваши 3 тестовых пода
```

## Устранение проблем

### Проблема: "Unable to connect to the server"

**Решение:**
- Проверьте, что kubeconfig файл правильный
- Убедитесь, что токен не истек (в Sandbox токены могут истекать)
- Попробуйте скачать новый kubeconfig

### Проблема: "Forbidden" или "Unauthorized"

**Решение:**
- Токен мог истечь - получите новый через веб-консоль
- Убедитесь, что используете правильный namespace
- Проверьте права доступа в Sandbox

### Проблема: Сервис не видит поды

**Решение:**
1. Проверьте namespace: `kubectl get pods -n ваш-namespace`
2. Убедитесь, что в `application.yml` указан правильный namespace
3. Проверьте логи сервиса на наличие ошибок

### Проблема: "Certificate signed by unknown authority"

**Решение:**
- В `OpenShiftConfig.java` уже включен `withTrustCerts(true)`
- Если проблема остается, проверьте настройки прокси/файрвола

## Быстрая проверка

Выполните эти команды для быстрой проверки:

```powershell
# 1. Проверьте подключение
kubectl get nodes

# 2. Найдите ваш namespace
kubectl get namespaces | Select-String "ваш-username"

# 3. Проверьте ваши поды
kubectl get pods -n ваш-namespace

# 4. Запустите сервис
$env:KUBECONFIG = "C:\Users\salof\.kube\sandbox-config"
mvn spring-boot:run

# 5. В другом терминале протестируйте
curl http://localhost:8080/api/pods/ваш-namespace
```

## Дополнительная информация

- **Red Hat Sandbox документация:** https://developers.redhat.com/developer-sandbox
- **Веб-консоль OpenShift:** Обычно доступна по ссылке из вашего аккаунта Sandbox
- **Срок действия токена:** Токены в Sandbox могут истекать, периодически обновляйте kubeconfig

## Следующие шаги

После успешного подключения вы можете:
1. ✅ Тестировать все API endpoints с реальными подами
2. ✅ Перезапускать поды через API
3. ✅ Удалять поды (полное отключение)
4. ✅ Мониторить состояние подов
5. 🔄 Расширить функциональность (управление Deployment'ами, ConfigMaps и т.д.)

